{
  "buildCommand": "npm install --legacy-peer-deps && expo export -p web",
  "outputDirectory": "dist",
  "devCommand": "expo",
  "cleanUrls": true,
  "framework": null,
  "installCommand": "npm install --legacy-peer-deps",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}
